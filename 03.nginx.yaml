- name: installing nginx and run
  hosts: web
  tasks:
  - name: install nginx
    ansible.builtin.package:
      name: nginx
      state: present
      become: yes


  - name: run nginx
    ansible.builtin.service:
      name: nginx
      state: started